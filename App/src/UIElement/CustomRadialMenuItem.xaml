<RadialMenu:RadialMenuItem x:Class="Taction.UIElement.CustomRadialMenuItem"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:local="clr-namespace:Taction"
		xmlns:UIElement="clr-namespace:Taction.UIElement"
		xmlns:RadialMenu="clr-namespace:RadialMenu.Controls;assembly=RadialMenu"
		mc:Ignorable="d"
		x:Name="LayoutRoot"
		d:DesignHeight="300" d:DesignWidth="300">
	<RadialMenu:RadialMenuItem.Resources>
		<Style TargetType="UIElement:CustomRadialMenuItem">
			<!-- Default values -->
			<Setter Property="InnerRadius" Value="{Binding Base_InnerRadius, TargetNullValue=70}" />
			<Setter Property="OuterRadius" Value="{Binding Base_OuterRadius, TargetNullValue=120}" />
			<Setter Property="Background" Value="{Binding Base_Background, TargetNullValue=#FFDDDDDD}" />
			<Setter Property="Padding" Value="{Binding Base_Padding, TargetNullValue=0}" />
			<Setter Property="BorderThickness" Value="{Binding Base_BorderThickness, TargetNullValue=1}" />
			<Setter Property="BorderBrush" Value="{Binding Base_BorderBrush, TargetNullValue=#FFDDDDDD}" />

			<Setter Property="EdgeInnerRadius" Value="{Binding Base_EdgeInnerRadius, TargetNullValue=120}" />
			<Setter Property="EdgeOuterRadius" Value="{Binding Base_EdgeOuterRadius, TargetNullValue=10000}" />
			<Setter Property="EdgeBackground" Value="{Binding Base_EdgeBackground, TargetNullValue=#0000}" />
			<Setter Property="EdgeBorderBrush" Value="{Binding Base_EdgeBorderBrush, TargetNullValue=#000}" />
			<Setter Property="EdgeBorderThickness" Value="{Binding Base_EdgeBorderThickness, TargetNullValue=0}" />
			<Setter Property="EdgePadding" Value="{Binding Base_EdgePadding, TargetNullValue=0}" />

			<Setter Property="ArrowBackground" Value="#0000" />
			<Setter Property="ArrowBorderBrush" Value="#0000" />
			<Setter Property="ArrowBorderThickness" Value="0" />
			<Setter Property="ArrowHeight" Value="10" />
			<Setter Property="ArrowRadius" Value="200" />
			<Setter Property="ArrowWidth" Value="10" />

			<Setter Property="ContentRadius" Value="{Binding Base_ContentRadius, TargetNullValue=95}" />
			<Setter Property="Base_LabelWidth" Value="50" />
			<Setter Property="Base_LabelHeight" Value="50" />
			<Setter Property="Base_LabelBackground" Value="#0000" />
			<Setter Property="Base_LabelForeground" Value="#FF222222" />

			<Style.Resources>
				<Style TargetType="TextBlock">
					<Style.Triggers>
						<DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type RadialMenu:RadialMenuItem}}, Path=IsMouseOver}" Value="True">
							<Setter Property="Foreground" Value="#FFFF0000" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
				<Style TargetType="Border">
					<Setter Property="Background" Value="{Binding Base_LabelBackground}" />
					<Setter Property="BorderBrush" Value="{Binding Base_LabelBorderBrush}" />
					<Setter Property="BorderThickness" Value="{Binding Base_LabelBorderThickness}" />
					<Setter Property="CornerRadius" Value="{Binding Base_LabelCornerRadius}" />
					<Setter Property="Margin" Value="{Binding Base_LabelMargin}" />
					<Setter Property="Padding" Value="{Binding Base_LabelPadding}" />
					<Setter Property="TextBlock.FontFamily" Value="{Binding Base_LabelFontFamily}" />
					<Setter Property="TextBlock.FontSize" Value="{Binding Base_LabelFontSize}" />
					<Setter Property="TextBlock.FontWeight" Value="{Binding Base_LabelFontWeight}" />
					<Setter Property="TextBlock.Foreground" Value="{Binding Base_LabelForeground}" />
					<Style.Triggers>
						<DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type RadialMenu:RadialMenuItem}}, Path=IsMouseOver}" Value="True">
							<Setter Property="Background" Value="{Binding Active_LabelBackground}" />
							<Setter Property="BorderBrush" Value="{Binding Active_LabelBorderBrush}" />
							<Setter Property="BorderThickness" Value="{Binding Active_LabelBorderThickness}" />
							<Setter Property="CornerRadius" Value="{Binding Active_LabelCornerRadius}" />
							<Setter Property="Margin" Value="{Binding Active_LabelMargin}" />
							<Setter Property="Padding" Value="{Binding Active_LabelPadding}" />
							<Setter Property="TextBlock.FontFamily" Value="{Binding Active_LabelFontFamily}" />
							<Setter Property="TextBlock.FontSize" Value="{Binding Active_LabelFontSize}" />
							<Setter Property="TextBlock.FontWeight" Value="{Binding Active_LabelFontWeight}" />
							<Setter Property="TextBlock.Foreground" Value="{Binding Active_LabelForeground}" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
				<Style TargetType="ContentPresenter">
					<Setter Property="Content" Value="{Binding Base_LabelContent}" />
					<Style.Triggers>
						<DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type RadialMenu:RadialMenuItem}}, Path=IsMouseOver}" Value="True">
							<Setter Property="Content" Value="{Binding Active_LabelContent}" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</Style.Resources>

			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="InnerRadius" Value="{Binding Active_InnerRadius}" />
					<Setter Property="Background" Value="{Binding Active_Background}" />
					<Setter Property="Padding" Value="{Binding Active_Padding}" />
					<Setter Property="BorderThickness" Value="{Binding Active_BorderThickness}" />
					<Setter Property="BorderBrush" Value="{Binding Active_BorderBrush}" />

					<Setter Property="OuterRadius" Value="{Binding Active_OuterRadius}" />
					<Setter Property="EdgeBackground" Value="{Binding Active_EdgeBackground}" />
					<Setter Property="EdgeBorderBrush" Value="{Binding Active_EdgeBorderBrush}" />
					<Setter Property="EdgeBorderThickness" Value="{Binding Active_EdgeBorderThickness}" />
					<Setter Property="EdgeInnerRadius" Value="{Binding Active_EdgeInnerRadius}" />
					<Setter Property="EdgeOuterRadius" Value="{Binding Active_EdgeOuterRadius}" />
					<Setter Property="EdgePadding" Value="{Binding Active_EdgePadding}" />

					<Setter Property="ContentRadius" Value="{Binding Active_ContentRadius}" />
					<Setter Property="Base_LabelWidth" Value="{Binding Active_LabelWidth}" />
					<Setter Property="Base_LabelHeight" Value="{Binding Active_LabelHeight}" />
					<Setter Property="Base_LabelBackground" Value="{Binding Active_LabelBackground}" />
					<Setter Property="Base_LabelForeground" Value="{Binding Active_LabelForeground}" />
				</Trigger>
			</Style.Triggers>
		</Style>
	</RadialMenu:RadialMenuItem.Resources>
	<Grid HorizontalAlignment="Stretch"
			VerticalAlignment="Stretch"
			Width="{Binding Base_LabelWidth}"
			Height="{Binding Base_LabelHeight}">
		<Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
			<ContentPresenter x:Name="Content" HorizontalAlignment="Center" VerticalAlignment="Center" />
		</Border>
	</Grid>
</RadialMenu:RadialMenuItem>
