<RadialMenu:RadialMenuItem x:Class="Taction.UIElement.CustomRadialMenuItem"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:local="clr-namespace:Taction"
		xmlns:UIElement="clr-namespace:Taction.UIElement"
		xmlns:RadialMenu="clr-namespace:RadialMenu.Controls;assembly=RadialMenu"
		mc:Ignorable="d"
		x:Name="LayoutRoot"
		d:DesignHeight="300" d:DesignWidth="300">
	<RadialMenu:RadialMenuItem.Resources>
		<Style TargetType="UIElement:CustomRadialMenuItem">
			<!-- Default values -->
			<Setter Property="InnerRadius" Value="70" />
			<Setter Property="Background" Value="#FFDDDDDD" />
			<Setter Property="Padding" Value="0" />
			<Setter Property="BorderThickness" Value="1" />
			<Setter Property="BorderBrush" Value="#FFDDDDDD" />

			<Setter Property="OuterRadius" Value="120" />
			<Setter Property="EdgeBackground" Value="#0000" />
			<Setter Property="EdgeBorderBrush" Value="#000" />
			<Setter Property="EdgeBorderThickness" Value="0" />
			<Setter Property="EdgeInnerRadius" Value="120" />
			<Setter Property="EdgeOuterRadius" Value="10000" />
			<Setter Property="EdgePadding" Value="0" />

			<Setter Property="ArrowBackground" Value="#0000" />
			<Setter Property="ArrowBorderBrush" Value="#0000" />
			<Setter Property="ArrowBorderThickness" Value="0" />
			<Setter Property="ArrowHeight" Value="10" />
			<Setter Property="ArrowRadius" Value="200" />
			<Setter Property="ArrowWidth" Value="10" />

			<Setter Property="ContentRadius" Value="95" />
			<Setter Property="IconWidth" Value="50" />
			<Setter Property="IconHeight" Value="50" />
			<Setter Property="IconBackground" Value="#0000" />
			<Setter Property="IconForeground" Value="#FF222222" />

			<Style.Resources>
				<Style TargetType="TextBlock">
					<Style.Triggers>
						<DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type RadialMenu:RadialMenuItem}}, Path=IsMouseOver}" Value="True">
							<Setter Property="Foreground" Value="#FFFF0000" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
				<Style TargetType="Border">
					<Setter Property="Background" Value="{Binding IconBackground}" />
					<Setter Property="BorderBrush" Value="{Binding IconBorderBrush}" />
					<Setter Property="BorderThickness" Value="{Binding IconBorderThickness}" />
					<Setter Property="CornerRadius" Value="{Binding IconCornerRadius}" />
					<Setter Property="Margin" Value="{Binding IconMargin}" />
					<Setter Property="Padding" Value="{Binding IconPadding}" />
					<Setter Property="TextBlock.FontFamily" Value="{Binding IconFontFamily}" />
					<Setter Property="TextBlock.FontSize" Value="{Binding IconFontSize}" />
					<Setter Property="TextBlock.FontWeight" Value="{Binding IconFontWeight}" />
					<Setter Property="TextBlock.Foreground" Value="{Binding IconForeground}" />
					<Style.Triggers>
						<DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type RadialMenu:RadialMenuItem}}, Path=IsMouseOver}" Value="True">
							<Setter Property="Background" Value="{Binding Active_IconBackground}" />
							<Setter Property="BorderBrush" Value="{Binding Active_IconBorderBrush}" />
							<Setter Property="BorderThickness" Value="{Binding Active_IconBorderThickness}" />
							<Setter Property="CornerRadius" Value="{Binding Active_IconCornerRadius}" />
							<Setter Property="Margin" Value="{Binding Active_IconMargin}" />
							<Setter Property="Padding" Value="{Binding Active_IconPadding}" />
							<Setter Property="TextBlock.FontFamily" Value="{Binding Active_IconFontFamily}" />
							<Setter Property="TextBlock.FontSize" Value="{Binding Active_IconFontSize}" />
							<Setter Property="TextBlock.FontWeight" Value="{Binding Active_IconFontWeight}" />
							<Setter Property="TextBlock.Foreground" Value="{Binding Active_IconForeground}" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
				<Style TargetType="ContentPresenter">
					<Setter Property="Content" Value="{Binding IconContent}" />
					<Style.Triggers>
						<DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type RadialMenu:RadialMenuItem}}, Path=IsMouseOver}" Value="True">
							<Setter Property="Content" Value="{Binding Active_IconContent}" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</Style.Resources>

			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="InnerRadius" Value="{Binding Active_InnerRadius}" />
					<Setter Property="Background" Value="{Binding Active_Background}" />
					<Setter Property="Padding" Value="{Binding Active_Padding}" />
					<Setter Property="BorderThickness" Value="{Binding Active_BorderThickness}" />
					<Setter Property="BorderBrush" Value="{Binding Active_BorderBrush}" />

					<Setter Property="OuterRadius" Value="{Binding Active_OuterRadius}" />
					<Setter Property="EdgeBackground" Value="{Binding Active_EdgeBackground}" />
					<Setter Property="EdgeBorderBrush" Value="{Binding Active_EdgeBorderBrush}" />
					<Setter Property="EdgeBorderThickness" Value="{Binding Active_EdgeBorderThickness}" />
					<Setter Property="EdgeInnerRadius" Value="{Binding Active_EdgeInnerRadius}" />
					<Setter Property="EdgeOuterRadius" Value="{Binding Active_EdgeOuterRadius}" />
					<Setter Property="EdgePadding" Value="{Binding Active_EdgePadding}" />

					<Setter Property="ContentRadius" Value="{Binding Active_ContentRadius}" />
					<Setter Property="IconWidth" Value="{Binding Active_IconWidth}" />
					<Setter Property="IconHeight" Value="{Binding Active_IconHeight}" />
					<Setter Property="IconBackground" Value="{Binding Active_IconBackground}" />
					<Setter Property="IconForeground" Value="{Binding Active_IconForeground}" />
				</Trigger>
			</Style.Triggers>
		</Style>
	</RadialMenu:RadialMenuItem.Resources>
	<Grid HorizontalAlignment="Stretch"
			VerticalAlignment="Stretch"
			Width="{Binding IconWidth}"
			Height="{Binding IconHeight}">
		<Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
			<ContentPresenter x:Name="Content" HorizontalAlignment="Center" VerticalAlignment="Center" />
		</Border>
	</Grid>
</RadialMenu:RadialMenuItem>
